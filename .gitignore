node_modules
dist
dist-ssr
*.local
.DS_Store
.vite
*.tsbuildinfo
.env
.env.*
!.env.example

# The 8 product videos are committed (~14 MB total). If they grow, move them
# to a CDN and re-add an ignore rule here — a rule alone would not untrack
# files that are already in history.
